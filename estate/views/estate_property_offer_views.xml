<odoo>
    <record id="view_estate_property_offer_search" model="ir.ui.view">
        <field name="name">estate.property.offer.search</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <search string="Search Offers">
                <field name="partner_id"/>
                <field name="price"/>
                <filter name="new" string="New" domain="[('status', '=', 'new')]"/>
                <filter name="accepted" string="Accepted" domain="[('status', '=', 'accepted')]"/>
            </search>
        </field>
    </record>


    <record id="view_estate_property_offer_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Property Offer">
                <group>
                    <field name="price"/>
                    <field name="partner_id"/>
                    <field name="validity"/>
                    <field name="date_deadline"/>
                    <field name="status"/>

                    <button name="action_accept_offer" type="object" icon="fa-check" string="Accept" invisible="status == 'accepted' or status == 'refused'"/>

                    <button name="action_refuse_offer" type="object" icon="fa-times" string="Refuse" invisible="status == 'accepted' or status == 'refused'"/>


                </group>
            </form>
        </field>
    </record>

    <record id="view_estate_property_offer_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Property Offers" decoration-danger="status == 'refused'" decoration-success="status == 'accepted'" editable="bottom">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="property_type_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <!-- Include status only to apply decoration rules -->
                <field name="status" invisible="1"/>

                <button name="action_accept_offer" type="object" icon="fa-check" string="Accept" invisible="status == 'accepted' or status == 'refused'"/>

                <button name="action_refuse_offer" type="object" icon="fa-times" string="Refuse" invisible="status == 'accepted' or status == 'refused'"/>

            </list>
        </field>
    </record>

    <record id="action_estate_property_offer" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_id.property_type_id', '=', active_id), ('status', '=', 'new')]</field>

        <!-- <field name="domain">[('property_type_id', '=', active_id)]</field> -->
    </record>

</odoo>
