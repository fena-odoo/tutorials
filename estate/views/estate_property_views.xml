<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Search View -->
    <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search Properties">
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="facades"/>
                <field name="state"/>

                <!-- Filters -->
                <!-- <filter string="Include archived" name="archived" domain="['|', ('active', '=', True), ('active', '=', False)]"/>
                <separator/>
                <filter string="Archived Only" name="archived_only" domain="[('active', '=', False)]"/>
                <separator/> -->
                <filter string="New Properties" name="new_only" domain="[('state', '=', 'new')]"/>
                <filter string="Offer Received" name="offer_received_only" domain="[('state', '=', 'offer_received')]"/>
                <filter name="available" string="Available" domain="[('state', 'in', ('new', 'offer_received'))]"/>
                <field name="living_area" string="Min. Living Area" filter_domain="[('living_area', '>=', self)]"/>

                <!-- Group By -->
                <group expand="1" string="Group By">
                    <filter name="group_by_state" string="State" context="{'group_by': 'state'}"/>
                    <filter name="group_by_postcode" string="Postcode" context="{'group_by': 'postcode'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- List view -->
    <record id="view_estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list decoration-success="state in ('offer_received', 'offer_accepted')" decoration-muted="state == 'sold'">
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="state"/>
                <field name="date_availability" optional="show" invisible="1"/>
            </list>
        </field>
    </record>

    <!-- Form view -->
    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button name="action_mark_sold" string="Mark as Sold" type="object" class="btn btn-primary" icon="fa-check" invisible="state in ('sold', 'cancelled')"/>

                    <button name="action_cancel_property" string="Cancel" type="object" class="btn btn-secondary" icon="fa-times" invisible="state in ('sold', 'cancelled')"/>

                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold,cancelled"/>

                </header>

                <sheet>
                    <!-- Title section -->
                    <div class="oe_title">
                        <h1>
                            <field name="name" class="mb16"/>
                        </h1>
                    </div>

                    <!-- Top two columns: postcode/date & expected/selling -->
                    <group>
                        <group>
                            <field name="state" readonly="1"/>
                            <field name="property_type_id" options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                            <field name="invoice_id"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price" readonly="1"/>
                            <field name="selling_price"/>
                            <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                        </group>
                    </group>

                    <!-- Notebook with Description tab -->
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area" readonly="1"/>
                            </group>
                        </page>

                        <page string="Offers">
                            <field name="offer_ids" context="{'default_partner_id': uid}" readonly="state in ('offer_accepted', 'sold', 'cancelled')">
                                <list editable="bottom" decoration-danger="status == 'refused'" decoration-success="status == 'accepted'">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <field name="status"/>
                                    <!-- Accept button -->
                                    <button name="action_accept_offer" type="object" icon="fa-check" string="Accept" invisible="status == 'accepted' or status == 'refused'"/>

                                    <!-- Reject button -->
                                    <button name="action_refuse_offer" type="object" icon="fa-times" string="Refuse" invisible="status == 'accepted' or status == 'refused'"/>

                                </list>
                                <form>
                                    <group>
                                        <field name="price"/>
                                        <field name="partner_id"/>
                                        <field name="validity"/>
                                        <field name="date_deadline"/>
                                        <field name="status"/>

                                        <button name="action_accept_offer" type="object" icon="fa-check" string="Accept" invisible="status in ('accepted', 'refused')"/>

                                        <button name="action_refuse_offer" type="object" icon="fa-times" string="Refuse" invisible="status in ('accepted', 'refused')"/>
                                    </group>
                                </form>
                            </field>
                        </page>


                        <page string="Contacts">
                            <group>
                                <field name="buyer_id"/>
                                <field name="salesperson_id"/>
                            </group>
                        </page>
                    </notebook>

                </sheet>
            </form>
        </field>
    </record>

    <!-- Kanban view -->
    <record id="view_estate_property_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" group_create="0" records_draggable="0">
                <!-- Needed but not shown fields -->
                <field name="state"/>
                <field name="best_price"/>
                <field name="selling_price"/>
                <field name="tag_ids"/>

                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_card">
                            <div>
                                <strong>
                                    <field name="name"/>
                                </strong>
                            </div>

                            <div>
                            Expected price: <field name="expected_price"/>
                            </div>

                            <div t-if="record.state.raw_value == 'offer_received'">
                            Best Offer: <field name="best_price"/>
                            </div>

                            <div t-if="record.state.raw_value == 'offer_accepted'">
                            Selling Price: <field name="selling_price"/>
                            </div>

                            <div>
                                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>


    <!-- Action -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="search_view_id" ref="view_estate_property_search"/>
        <field name="context">{'search_default_available': 1}</field>
    </record>
</odoo>
